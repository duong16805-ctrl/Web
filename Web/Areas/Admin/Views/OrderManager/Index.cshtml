@model IEnumerable<Web.Models.Order>
@{
    ViewBag.Title = "Quản lý Đơn hàng";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid px-4">
    <h2 class="mt-4 mb-4">Quản lý Đơn hàng</h2>

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-light text-muted text-uppercase small">
                        <tr>
                            <th class="ps-4">Mã ĐH</th>
                            <th>Ngày đặt</th>
                            <th>Tổng tiền</th>
                            <th>Trạng thái</th>
                            <th class="text-end pe-4">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4">#@item.OrderID</td>
                                <td>@item.OrderDate.ToString("dd/MM/yyyy HH:mm")</td>
                                <td class="fw-bold text-dark">@string.Format("{0:N0} đ", item.TotalAmount)</td>
                                <td>
                                    @using (Html.BeginForm("UpdateStatus", "OrderManager", new { id = item.OrderID }, FormMethod.Post))
                                    {
                                        <select name="status" onchange="this.form.submit()" class="form-select form-select-sm w-auto">
                                            <option value="Pending" @(item.Status == "Pending" ? "selected" : "")>Chờ duyệt</option>
                                            <option value="Processing" @(item.Status == "Processing" ? "selected" : "")>Đang làm</option>
                                            <option value="Completed" @(item.Status == "Completed" ? "selected" : "")>Hoàn thành</option>
                                            <option value="Cancelled" @(item.Status == "Cancelled" ? "selected" : "")>Đã hủy</option>
                                        </select>
                                    }
                                </td>
                                <td class="text-end pe-4">
                                    <a href="@Url.Action("Details", new { id = item.OrderID })" class="btn btn-sm btn-info text-white">
                                        <i class="fas fa-eye me-1"></i> Chi tiết
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>